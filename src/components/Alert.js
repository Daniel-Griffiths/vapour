import styled from 'styled-components'

export default styled.div`
	margin: 1rem;
	padding: 1rem;
	display: block;
	text-align:center;
	font-weight: bold;
	border-radius: 3px;
	background: var(--red);
`